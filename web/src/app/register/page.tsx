import { AuthForm } from "@/components/auth/auth-form";

export const metadata = { title: "가입 — Personal Color AI" };

export default function RegisterPage() {
  return <AuthForm mode="register" />;
}
