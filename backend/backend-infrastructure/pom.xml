<?xml version="1.0" encoding="UTF-8"?>
<!--
  인프라 모듈 — 도메인 포트의 구현체(어댑터)들이 사는 곳.

  JPA 엔티티, Redis 캐시, ml-service를 부르는 WebClient, Flyway
  마이그레이션이 전부 여기 있다. 도메인이 정의한 인터페이스를 구현할 뿐
  도메인을 바꾸지는 않는다.

  JPA 엔티티를 도메인이 아니라 여기 두는 이유: 엔티티는 "DB에 이렇게
  저장한다"는 인프라 결정이지 도메인 개념이 아니다. 매핑 보일러플레이트가
  생기는 대신, 컬럼 하나 바꾸는 일이 도메인 모델을 건드리지 않는다.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.personalcolor</groupId>
    <artifactId>backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>backend-infrastructure</artifactId>
  <name>backend-infrastructure</name>
  <description>영속화·캐시·외부 서비스 어댑터</description>

  <dependencies>
    <dependency>
      <groupId>com.personalcolor</groupId>
      <artifactId>backend-domain</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <!-- Boot 4에서는 webflux 스타터가 jackson-databind를 더 이상 끌어오지
         않는다. ml-service 응답 역직렬화와 Redis JSON 직렬화 양쪽이
         필요로 하므로 명시적으로 선언한다. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-json</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <!-- ml-service 호출용 WebClient. 서블릿 스택에서도 WebClient는
         RestClient보다 타임아웃·재시도 제어가 세밀해 외부 호출에 쓴다. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-circuitbreaker</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-timelimiter</artifactId>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
